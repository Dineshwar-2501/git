👉Git Commands :
** OPEN THE FOLDER WHER GIT SHOULD BE CONNECTED **
** LOCAL **
Initialize Git                                      :        git inti 
Add Files                                           :        git add . (or) git add file1 file2 ...
Commit Changes                                      :        git commit -m "Initial commit" 
Set Up Remote                                       :        git remote add origin <remote_repository_URL>
Remove the remote                                   :        git remote remove origin
Push Changes to Remote                              :        git push -u origin master
Clone a repository                                  :        git clone <repository_URL>
Clone a repository of a certain file                :        git clone <repository_URL> <directory_name>
Check Repository Status                             :        git status
Commit Changes                                      :        git commit -m "Commit message"
View Commit History                                 :        git log
Create a New Branch                                 :        git branch <branch_name>
Switch to a Branch                                  :        git checkout <branch_name>
Merge Changes from One Branch to Another            :        git merge <branch_name>
Fetch Changes from a Remote Repository              :        git fetch <remote_name>
Pull Changes from a Remote Repository               :        git pull <remote_name> <branch_name>
Push Changes to a Remote Repository                 :        git push <remote_name> <branch_name>
Add a Remote Repository                             :        git remote add <remote_name> <repository_URL>
Undo Local Changes (before staging)                 :        git checkout -- <file_name>
Undo Local Changes (after staging)                  :        git reset HEAD <file_name>|&|git checkout -- <file_name>
Undo Last Commit (discard changes)                  :        git reset --hard HEAD~1
Add a file which inside a file                      :        git submodule add <url> <file_inside_name>
To Remove Files                                     :        git rm <file>
To Move or Rename Files                             :        git mv <old_path> <new_path>

CONFIG:-
git config --global user.name "Your Name"
git config --global user.email "Your Email"
git config --global init.defaultBranch main

AFTER EDITING A CLONED REPO (create a repo in github)
git remote set-url origin http://github.com/Dineshwar-2501/[repo name]
git branch -M main
git push -u origin main
